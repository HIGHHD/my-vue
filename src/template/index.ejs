<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <title>SCZQ</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <% htmlWebpackPlugin.files.css.forEach(function(e) { %>
      <link rel="stylesheet" href="<%= e %>">
    <% }); %>
</head>

<body>
    <div id="app"></div>

    <% htmlWebpackPlugin.files.js.forEach(function(e) { %>
      <script type="text/javascript" src="<%= e %>"></script>
    <% }); %>
</body>
</html>